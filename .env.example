# PostgreSQL Performance Analysis - Email Configuration (OPTIONAL)
# Copy this file to .env and fill in your values to enable email reports

# Email addresses
EMAIL_FROM=your-email@example.com
EMAIL_TO=recipient1@example.com,recipient2@example.com

# SMTP server configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# SMTP authentication
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-app-password-here

# Notes:
# - For Gmail, use an App Password (not your regular password)
# - Generate App Password: Google Account > Security > 2-Step Verification > App passwords
# - Multiple recipients: separate with commas (no spaces)
# - Common SMTP ports: 587 (TLS), 465 (SSL), 25 (unencrypted)
